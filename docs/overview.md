# File Sharer Overview

This project transfers files across a network using Java socket programming.
RSA is used to exchange trust material, and AES is used to encrypt the actual file transfer session.

The repository is organized as a Maven project, with source under `src/main/java`.
